function addition(a, b) {
  const first = a;
  const second = b;
  const operator = "+";
  const total = a + b;
  return total;
}

function subtract(a, b) {
  const first = a;
  const second = b;
  const operator = "-";
  const total = a - b;
  return total;
}

function multiply(a, b) {
  const first = a;
  const second = b;
  const operator = "*";
  const total = a * b;
  return total;
}

function divide(a, b) {
  const first = a;
  const second = b;
  const operator = "/";
  const total = a / b;
  return total;
}